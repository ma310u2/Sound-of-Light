
$(window).on('load resize', function () {
    var w = $(window).width();
    var x = 883;
    var $a = $("a");
    if (w <= x) {
       $hil.hide().off("mouseover").off("mouseout");
    } else {
        $a.mouseover(function () {
            $("#sub-list", this).stop().slideDown();
        })
        $a.mouseout(function () {
            $("#sub-list", this).stop().slideUp();
        });
    }
    $("#sub-list").hide();

});
